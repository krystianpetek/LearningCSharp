<Window x:Class="Zadanie_4._01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zadanie_4._01"
        mc:Ignorable="d"
        Title="Panel Canvas" Height="200" Width="300">
    <Grid>

        <!--
            <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0" Height="15" Margin="5,0,5,0">
            <Button Width="15"
                    Height="15"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top" />
        </Canvas>-->

        <Canvas Grid.Row="1">
            <Button Content="Domyślny" Panel.ZIndex="2" />
            <Button Content="Top 30" Canvas.Top="30" />
            <Button Content="Right 10" Canvas.Right="10" />
            <Button Content="Top Left" Canvas.Top="15" Canvas.Left="20" Panel.ZIndex="1" />
            <Button Content="Bottom Right" Canvas.Bottom="10" Canvas.Right="10" />
        </Canvas>
    </Grid>
</Window>